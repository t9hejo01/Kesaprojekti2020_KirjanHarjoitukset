dictionary = {'variable': 'Every variable is connected to a value, which is the information associated with that variable.',
              'for loop': 'A for loop is used for iterating over sequence.',
              'string': 'A string is a series of characters.',
              'list': 'A list is a collection of items in a particular order.',
              'tuple': 'A tuple is a collection which is ordered and unchangeable.',
              'argument': 'A value passed to a function (or method) when calling the function.',
              'class': 'A template for creating user-defined objects.',
              'dictionary': 'An associative array, where arbitrary keys are mapped to values.',
              'f-string': 'A short for formatted string literals.',
              'function': 'A series of statements which returns some value to a caller.'}

                
for word, meaning in dictionary.items():
    print(f"\n{word.title()}")
    print(f"\t{meaning}")
    
    



